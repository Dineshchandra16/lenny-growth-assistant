/**
 * components/Artifact/ArtifactViewer.tsx
 * ────────────────────────────────────────
 * Phase 5 stub — artifact rendering is implemented in Phase 5.
 *
 * In Phase 5 this component will:
 * - Detect artifact type (markdown | html)
 * - Render Markdown via react-markdown + remark-gfm
 * - Render HTML in a DOMPurify-sanitized sandboxed iframe
 * - Show copy/download controls
 */

export default function ArtifactViewer() {
  return null; // Phase 5
}
